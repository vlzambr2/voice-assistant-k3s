apiVersion: v1
kind: Service
metadata:
  name: faster-whisper-service
  namespace: voice-assistant
spec:
  type: ClusterIP
  selector:
    app: faster-whisper
  ports:
    - targetPort: 10300
      port: 10300
      name: faster-whisper-api